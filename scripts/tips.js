let tipsData = []


class Tip {

    constructor(id, title, text) {
  
      this.id = id;
      this.title = title;
      this.text = text;
  
    }
  
  }
  

tipsData[1] = new Tip(1, "2 digit numbers multiplication", "If you have two 2 digit numbers that you want to multiply, you can decompose into easier multiplications, like this: \r\n 22x58 = 22 x (50 + 8) \r\n 22 x (5 + 8) = 1100 + 178 = 1278");

tipsData[2] = new Tip(2, "example", "example");